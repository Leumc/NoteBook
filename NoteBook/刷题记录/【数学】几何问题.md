---
知识点:
  - 几何
题目链接: 暂时没有
难度(洛谷): 不知道
---
# 题目快照：

![[微信截图_20260228223446.png|697]]
# 问题分析：

肯定是判断在对角线相切时的边界条件，然后我写出了：

```cpp
#include <bits/stdc++.h>
using namespace std;
int t;
int main() {
	cin >> t;
	for (int i = 1; i <= t; i++) {
		int n, r1, r2; cin >> n >> r1 >> r2;
		double rv1 = sqrt(2.0 * r1 * r1), rv2 = sqrt(2.0 * r2 * r2);
		double a = r1 + r2 + rv1 + rv2, b = sqrt(2.0 * n * n);
		if (a < b)cout << "YES" << endl;
		else cout << "NO" << endl;
	}
}
```

只有30分，什么问题呢？请看：

![[几何啊几何.drawio.png|697]]

我能说什么呢？