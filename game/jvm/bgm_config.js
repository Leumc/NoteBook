// ================= BGM 播放列表配置 =================
// 由于浏览器环境无法直接读取本地目录文件列表，请在此配置每个常规阶段的文件名
const bgmConfig = {
    mainMenuTrack: 'bgm/Interrupt_Wait.mp3',
    phaseTracks: {
        1: ['Cold_Boot_Sequence.mp3', 'Silence_of_the_Heap.mp3'],
        2: ['Spin-Lock_Contention.mp3'],
        3: ['1.mp3', '2.mp3', '3.mp3']
    },
    // 新增：音乐播放器用的完整元数据列表
    playerTracks: [
        {
            title: "中断等待/Interrupt Wait",
            path: "bgm/Interrupt_Wait.mp3",
            usage: "主界面",
            desc: "在计算机体系结构中，“中断（Interrupt）”是操作系统与底层硬件之间进行异步通信的核心机制。与 CPU 按照固定顺序逐条取指、执行的常规流水线不同，中断允许外部设备（如键盘、网卡或定时器）在关键事件发生时，直接向 CPU 发送高优先级的硬件电平信号，强行打断其当前的指令执行流，使其优先处理突发任务。\n\n“中断等待”描述的是一种特殊的底层微观状态：在这一阶段，系统已经完成了所有前置的初始化与自检任务，但由于尚未接收到任何外部输入，任务队列处于完全清空的状态。此时，CPU 会执行特定的休眠指令（如 HLT 指令），主动挂起计算单元并进入极低功耗的休眠模式。这种等待状态意味着系统引擎已经全面就绪，各级总线接口保持着对外界信号的高度敏锐；它并非真正的死机或停滞，而是一种蓄势待发的静默，只等物理外设触发那一次决定性的电平跳变，便会瞬间唤醒内核，将系统拉入高强度的数据处理洪流中。"
        },
        {
            title: "冷启动序列/Cold Boot Sequence",
            path: "bgm/phase/phase1/Cold_Boot_Sequence.mp3",
            usage: "常规阶段 1",
            desc: "“冷启动序列”是指计算机系统从完全断电的物理静止状态，逐步苏醒并接管硬件控制权的整个底层初始化过程。当电源接通后，系统会从一个固定的物理地址读取第一条指令。\n\n系统首先唤醒主板上的固件，执行加电自检（POST），以验证 CPU、内存等核心硬件是否运转正常。确认无误后，固件会根据预设的启动顺序寻找引导设备，将存储在引导扇区中的引导加载程序读入内存。随后，引导加载程序负责将操作系统最核心的“内核（Kernel）”加载到内存中执行。内核接管系统后，会进一步初始化各种外设驱动程序，挂载根文件系统，启动后台系统服务，最终呈现出可交互的界面。这个从零开始、层层递进的接力过程，是解决“硬件如何加载并运行软件”这一根本问题的核心机制。"
        },
        {
            title: "堆区静默/Silence of the Heap",
            path: "bgm/phase/phase1/Silence_of_the_Heap.mp3",
            usage: "常规阶段 1",
            desc: "在计算机内存管理中，“堆（Heap）”是专门用于程序在运行期间动态分配和释放内存的广阔区域。与由编译器自动按序管理的栈（Stack）不同，堆区的内存管理极其复杂，充满了不规则的数据块和由于频繁分配释放而产生的内存碎片。\n\n“堆区静默”描述的是一种特殊的系统微观状态：在这一阶段，程序处于相对平稳的运行周期，几乎没有产生申请内存的系统调用，也没有大规模销毁旧的数据对象。此时，底层的内存分配器处于一种待命的休眠状态，不需要消耗 CPU 周期去遍历空闲链表或合并内存碎片。这种静默状态通常意味着系统当前的负载极低，数据结构处于高度稳定的平衡期；但在处理海量数据的系统中，这往往也是数据洪流即刻涌入、堆区即将面临剧烈扩张和大规模高频读写操作之前的短暂宁静。"
        },
        {
            title: "击穿 -Xmx/Breaching the -Xmx",
            path: "bgm/boss/boss1/Breaching_the_-Xmx.mp3",
            usage: "OOM-Reaper Boss战",
            desc: "虽然“-Xmx”这个词汇本身源自 Java 虚拟机中用于限制进程最大可用堆内存的启动参数，但“击穿”这一概念反映的是计算机科学中普遍存在的“内存溢出（Out of Memory, OOM）”危机。在任何现代操作系统中，程序在运行时都需要在内存的堆区（Heap）动态申请空间来存储数据。\n\n为了防止单个故障进程耗尽所有物理内存导致系统死机，操作系统或运行环境会为每个进程划定一个严格的虚拟内存或物理内存使用上限。当程序由于逻辑漏洞（如内存泄漏）或处理的数据量极其庞大，导致其申请的内存总量超出了这个最高警戒线时，系统的防线就会被“击穿”。此时，底层的内存分配器将无法提供更多空间，通常会抛出致命错误，操作系统甚至会直接强制杀死该进程，以保护整个系统的稳定。"
        },
        {
            title: "自旋锁争用/Spin-Lock Contention",
            path: "bgm/phase/phase2/Spin-Lock_Contention.mp3",
            usage: "常规阶段 2",
            desc: "在多线程并发编程中，锁机制用于保证多个线程在访问共享资源时不发生数据竞争（Race Condition）。传统的互斥锁（Mutex）在获取失败时，会让当前线程进入休眠状态，这会引发操作系统的上下文切换（Context Switch），带来较大的 CPU 性能开销。为了解决这个问题，“自旋锁”应运而生。当一个线程尝试获取已被占用的自旋锁时，它不会挂起自己，而是在一个循环中不断地执行空转（即“自旋”），高频检查锁是否已被释放。这种机制在锁被占用时间极短的情况下效率极高。\n\n然而，当系统处于高并发状态，大量线程同时试图获取同一把锁时，就会发生严重的“自旋锁争用”。此时，多个 CPU 核心被这些不断空转的线程占据，白白消耗大量计算资源却不执行任何有效逻辑，导致系统整体吞吐量严重下降。理解并减少自旋锁争用，是提升多核处理器并发性能的核心关键。"
        },
        {
            title: "死锁逃逸/Deadlock Evasion",
            path: "bgm/boss/boss2/Deadlock_Evasion.mp3",
            usage: "Boss战2（未实装）",
            desc: "死锁是计算机操作系统和并发程序设计中最棘手的问题之一。当两个或多个进程（或线程）各自占有一部分互斥资源，同时又在互相等待对方释放自己所需的其他资源时，就会形成一个闭环的逻辑死结。这会导致所有相关进程永远卡死，程序彻底停滞。\n\n为了解决这个致命问题，计算机科学家们提出了“死锁逃逸”（或称死锁避免）机制。这是一种动态的系统干预策略，其核心在于防患于未然。系统在每次分配资源之前，都会通过特定的算法（例如著名的银行家算法）进行严格的风险评估。系统会模拟资源分配后的状态，如果预判这次分配可能会导致未来陷入无法满足所有进程需求的“不安全状态”，系统就会果断拒绝该请求或让进程延迟等待。通过这种全局统筹与调度，操作系统能够在复杂的资源争夺中灵活周旋，逃避死锁陷阱。"
        }
    ]
};